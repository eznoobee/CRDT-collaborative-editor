# .NET
bin/
obj/
*.user
*.suo
artifacts/
TestResults/
StrykerOutput/
*.trx
*.coverage

# Node
node_modules/
dist/
.vite/
*.tsbuildinfo

# Environment / secrets — see PROJECT_SPEC.md §7
.env
.env.*
!.env.example
*.pfx
*.p12

# Editors
.vs/
.idea/
.vscode/*
!.vscode/extensions.json
*.swp
.DS_Store
dist-types/

# Browser metric harness dependencies (PROJECT_SPEC.md §8); the lockfile is kept.
tests/browser/node_modules/

# Scratch entry points written by tests/browser/bundle-metrics.mjs. They live
# inside the client because esbuild resolves a bare import from the importing
# file's directory, so an entry point outside it cannot see client/node_modules.
client/.bundle-metrics-*/
